<h1></h1>

<%= form_for @search do |f| %>
  <div class="field">
    <%= f.label :jobcategory %><br />
    <%= f.collection_select :jobcategory, Jobcategory.order(:name), :id, :name, include_blank: true %>
  </div>
    
   
  <div class="actions"><%= f.submit "Search" %></div>
<% end %>

