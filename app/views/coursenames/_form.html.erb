<%= form_for(@coursename) do |f| %>
  <% if @coursename.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coursename.errors.count, "error") %> prohibited this coursename from being saved:</h2>

      <ul>
      <% @coursename.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
