<h3><h align="left"><strong>List of Jobs</h3>

<%= form_tag jobs_new_path, :url => { :action => "create" } do |f| %>  

  <p> 
    <%= text_field_tag :search, params[:search]  %>
    <%= submit_tag "Search and display on a results page", :name => nil %>
  </p>
<% end %>

<%= form_tag jobs_path, :method => 'get' do %>

  <p>
    <%= text_field_tag :search, params[:search]  %>
    <%= submit_tag "Search and display below ", :name => nil %>
  </p>
<% end %>

<% for job in @jobs %>
   <%= link_to job.jobcategory, new_job_path(job) %>
<% end %>

<table>
  <thead>
    <tr>
      <th></th>
    </tr>
  </thead>

  <tbody>

<%= link_to 'New Job', new_job_path %>



<div jobcategory="pagination_table">
  <% Job.order("jobcategory").each do |job| %>
      <tr>
        <td><%= job.jobcategory %></td>
        <td><%= job.id %></td>
        <td><%= job.coursename %></td>
        <td><%= job.alevel %></td>
        <td><%= link_to 'Edit', job, method: :edit, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Destroy', job, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
    </div>
  </tbody>
</table>
  
<br>

<%= link_to 'New Job', new_job_path %>

